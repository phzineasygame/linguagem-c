o #include <stdio.h> é um comando que importa a biblioteca padrão de input e output da linguagem C, para usar funções como o print.

o programa começa na função main

tirar o return 0 pode trazer erros de compilação do código

se tirar o /n, o texto vai continuar na mesma linha, não vai pular outra linha, se mantendo na mesma.